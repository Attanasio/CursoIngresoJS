function mostrar()
{
    var pagoRealizado;
    var tipoDePago;
    var paqueteComprado;
    var descuento;

    pagoRealizado = prompt ("Ingrese el total de lo que tiene que pagar.");
    tipoDePago = prompt ("Ingrese con medio realizara el pago.");
    paqueteComprado = prompt ("Ingrese el tipo de paquete que deasea.");

    switch (tipoDePago)
    {
        case "tarjeta visa":
            descuento=(pagoRealizado*0.90);
        break;
            case ""

    }

}
